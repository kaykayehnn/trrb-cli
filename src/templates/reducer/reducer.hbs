import { {{name}}State } from '@Store/state/{{formatName dirname name}}.state'
import { {{name}}Action, {{name}}ActionTypes } from '@Store/actions/{{formatName dirname name}}.actions'

const initialState: {{name}}State

export function {{functionCase name}}Reducer (state = initialState, action: {{name}}ActionTypes): {{name}}State {
  switch (action.type) {
    default:
      return state
  }
}
