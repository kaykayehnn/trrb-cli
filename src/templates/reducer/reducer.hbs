import {{name}}State from '@Store/state/{{name}}.state'
import { {{name}}Actions, {{name}}Types } from '@Store/actions/{{name}}.actions'

const initialState: {{name}}State

export default function {{functionCase name}}Reducer (state = initialState, action: {{name}}Actions) {
  switch (action.type) {
    default:
      return state
  }
}
