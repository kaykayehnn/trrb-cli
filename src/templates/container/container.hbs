{{#if component}}
{{else}}
import React from 'react'
{{/if}}
import { connect } from 'react-redux'

{{#if component}}
import {{component}} from '@Components/{{component}}'
{{/if}}
import { MapStateToProps, MapDispatchToProps, MergeProps } from 'types/Redux'

export interface PropsFromState {}

export interface PropsFromDispatch {}

export type {{name}}Props = MergeProps<PropsFromState, PropsFromDispatch>

export const mapStateToProps: MapStateToProps<PropsFromState> = state => ({})

export const mapDispatchToProps: MapDispatchToProps<PropsFromDispatch> = {}

const {{name}} = connect(
  mapStateToProps,
  mapDispatchToProps
)({{#if component}}{{component}}{{else}}() => <h1>{{name}} component</h1>){{/if}}

export default {{name}}
