import React from 'react'
import { shallow } from 'enzyme'

import { {{name}} } from '@Components/{{formatName dirname name}}/'

describe('{{name}} Component', () => {
  it('renders self', () => {
    const wrapper = shallow(<{{name}} />)
  })
})
