import React from 'react'
import { render } from 'react-testing-library'

import { {{name}} } from '.'

describe('{{name}} Component', () => {
  it('renders', () => {
    render(<{{name}} />)
  })
})
